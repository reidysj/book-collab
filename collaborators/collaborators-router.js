/**
POST /books/:book_id/collaborators: Add a collaborator to a book.

GET /books/:book_id/collaborators: Retrieve a list of collaborators for a specific book.

DELETE /books/:book_id/collaborators/:user_id: Remove a collaborator from a book.
 */
